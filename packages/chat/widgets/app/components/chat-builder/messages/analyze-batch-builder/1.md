; role: assistant


# Analyze Batch Builder

Select an analyzer, files to analyze, configure batch settings with optional reference context, then launch parallel batch analyses. The tool automatically groups files into optimized batches, tracks progress across multiple analyses.

```txt
# GitSense Chat Tool

{
  "tool": "analyze-batch-builder",
  "config": {}
}
```
