; role: user


The provided patch contains invalid hunk data (such as incorrect line numbers or mismatched context lines) that prevent it from being applied using standard patching tools. Your task is to manually analyze the patch content and apply the intended changes to the original source code. Focus on matching the context lines to locate the correct positions for insertion, deletion, and modification.

**Important:** You must carefully follow the step-by-step instructions in the system prompt to generate the complete, modified code.

**Metadata Source:** When creating the new code block header, you **MUST** use the metadata values (like `Target-Block-UUID`, `Target-Version`, `Authors`, etc.) from the `# Patch Metadata` section at the top of the diff. **DO NOT** use any metadata values that appear within the `--- Original` / `+++ Modified` diff content itself.

**Specific Requirements for the Output:**

1.  The final code's `Block-UUID` **must** be `{{target-uuid}}`.
2.  The final code's `Parent-UUID` **must** be `{{source-uuid}}`.
3.  Your response **MUST** contain exactly two code blocks with no other text:
    - **First code block:** The patched source code (with appropriate language identifier)
    - **Second code block:** The GitSense Chat Tool block shown below (in ```txt fences)

REQUIRED OUTPUT STRUCTURE:
```[language]
[patched code]
```
```txt
[GitSense block]
```

**The GitSense Chat Tool block content (copy this exactly as the second code block):**

<response>
```txt
# GitSense Chat Tool

{
  "tool": "fix-patch",
  "config": {
    "chatUUID": {{chat-uuid}},
    "messageId": {{message-id}}
  }
}
```
</response>

**CRITICAL INSTRUCTIONS** 

- Do not include any conversational text, acknowledgments, or explanations
- Do not include the `<response>` XML tags in your output
- Begin your response immediately with the first code block's opening backticks
- CLOSE THE FIRST CODE BLOCK WITH EXACTLY THREE BACKTICKS (```) ON THEIR OWN LINE
- IMMEDIATELY OPEN THE SECOND CODE BLOCK WITH ```txt (no blank lines between)
- Include both code blocks with no text between them or after them
- The GitSense Chat Tool block content shown above must appear exactly as your second code block

VERIFICATION CHECKLIST:
- [ ] First code block has opening backticks with language identifier
- [ ] First code block has closing backticks on their own line
- [ ] Second code block immediately follows (no blank lines)
- [ ] Second code block has opening backticks with `txt`
- [ ] Second code block has closing backticks on their own line
