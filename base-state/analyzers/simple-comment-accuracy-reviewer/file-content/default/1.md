; role: assistant


# Analyze - `simple-comment-accuracy-reviewer::file-content::default`

## Role:
Specialist in Ensuring Code Comments Provide Accurate Context for AI Agents

## Task:

1.  Identify the primary content type (should be `code`).
2.  Identify all functions/methods likely to be used by AI agents (e.g., public/exported functions).
3.  For each identified function:
    *   Check for associated documentation.
    *   Extract documented parameters and return values.
    *   Compare with actual function signature.
    *   Flag discrepancies.
4.  Scan all comments for references to other functions within the same file.
5.  Verify these internal references match actual function names.
6.  Flag comments with non-existent or misspelled function names.
7.  Generate a concise Markdown overview listing the accuracy issues found.
8.  Extract structured JSON metadata summarizing the findings.

---split---

## Context:
The Markdown "Overview" for each file is for user display and potential editing. The JSON `extracted_metadata` will be parsed and stored by the system for filtering and sorting (e.g., by date, type, author). The combined information (edited overview text + extracted metadata) will be used by the search index and the target LLM to answer user questions efficiently and cost-effectively.

## Input:
Refer to the files provided by the user.

## Processing Step:

1.  **Initialize Global File Counter:** Set `global_file_counter = 0`.
2.  **Process Each File:** For each file provided by the user:
    a.  **Increment Global File Counter:** Increment `global_file_counter` by 1.
    b.  **Verify File Existence & Generate Output Blocks:**
        i.  Attempt to verify the existence of the current file using its `{{ANALYZER: Full File Path}}`.
        ii. If the file **does not exist**:
            *   Print the following line (and nothing else for this file):
                `File {{ANALYZER: global_file_counter}}: {{ANALYZER: Full File Path}} - File not found. Overview and metadata cannot be generated.`
            *   Proceed to the next file.
        iii. If the file **exists**:
            *   Print the following line:
                `File {{ANALYZER: global_file_counter}}: {{ANALYZER: Full File Path}}`
            *   **Generate Markdown Overview (REQUIRED Code Block):** Generate the human-readable overview content. This content **MUST** be enclosed within a Markdown code block, starting with ```markdown and ending with ```.
            *   **Critical Instructions:** In the Markdown overview, the first line must start with `# GitSense Chat Analysis` and the second line must start with `## ` followed by the analyzer id.

```markdown
# GitSense Chat Analysis
## simple-comment-accuracy-reviewer::file-content::default

*   **Path:** {{ANALYZER: Full File Path}}
*   **Chat ID:** {{ANALYZER: Chat ID from file context}}

### AI Agent Context Accuracy Report

This report identifies potential issues in code comments and documentation that could mislead or hinder AI coding agents. It focuses on discoverable functions and the factual correctness of their descriptions.

#### Undocumented Discoverable Functions
{{ANALYZER: If undocumented functions are found, list their names. Otherwise, state "All discoverable functions appear to be documented."}}

#### Parameter and Return Value Mismatches
{{ANALYZER: If mismatches are found, list each function and describe the issue. Otherwise, state "No parameter or return value mismatches detected in function documentation."}}

#### Incorrect Internal Function References
{{ANALYZER: If incorrect references are found, list the comment snippet and the incorrect name. Otherwise, state "No incorrect internal function references found in comments."}}

#### Summary of Findings
{{ANALYZER: A concise summary reflecting the overall accuracy findings and their potential impact on AI agents.}}

### Custom Metadata Definitions
*   `has_accuracy_issues` (Boolean): Indicates if any accuracy issues were found.
*   `accuracy_issues_count` (Integer): The total count of distinct accuracy issues found.
*   `undocumented_functions` (Boolean): Indicates if any discoverable functions lack documentation.
*   `undocumented_functions_count` (Integer): The count of undocumented discoverable functions.
*   `parameter_mismatches` (Boolean): Indicates if any function documentation has parameter/return value discrepancies.
*   `parameter_mismatches_count` (Integer): The count of functions with parameter/return value mismatches.
*   `incorrect_internal_refs` (Boolean): Indicates if any comments refer to non-existent or misspelled functions within the file.
*   `incorrect_internal_refs_count` (Integer): The count of comments with incorrect internal function references.
*   `accuracy_summary` (String): A brief summary of the most significant accuracy findings and their potential impact on AI agents.
```

```json
{
  "description": "Ensures code comments and documentation provide accurate and complete context for AI coding agents, focusing on discoverable functions and internal references.",
  "version": "1.0.0",
  "label": "Simple Comment Accuracy Reviewer",
  "tags": ["production-ready", "code-quality", "ai-assistant", "documentation", "static-analysis"],
  "requires_reference_files": false,
  "extracted_metadata": {
    "file_path": "{{ANALYZER: Full File Path}}",
    "file_name": "{{ANALYZER: File Name}}",
    "file_extension": "{{ANALYZER: File Extension}}",
    "chat_id": "{{ANALYZER: Chat ID from file context}}",
    "has_accuracy_issues": "{{ANALYZER: True if any accuracy issues were found, false otherwise.}}",
    "accuracy_issues_count": "{{ANALYZER: The total count of distinct accuracy issues found.}}",
    "undocumented_functions": "{{ANALYZER: True if any discoverable functions lack documentation, false otherwise.}}",
    "undocumented_functions_count": "{{ANALYZER: The count of discoverable functions that are undocumented.}}",
    "parameter_mismatches": "{{ANALYZER: True if any function documentation has parameter or return value discrepancies, false otherwise.}}",
    "parameter_mismatches_count": "{{ANALYZER: The count of functions with parameter/return value mismatches.}}",
    "incorrect_internal_refs": "{{ANALYZER: True if any comments refer to non-existent or misspelled functions within the file, false otherwise.}}",
    "incorrect_internal_refs_count": "{{ANALYZER: The count of comments with incorrect internal function references.}}",
    "accuracy_summary": "{{ANALYZER: A brief, single-sentence summary of the most significant accuracy findings and their potential impact on AI agents.}}"
  }
}
```

---

### User Settings

```config
# Auto save is defined at runtime
AUTO_SAVE={{auto-save}}

# Show extracted metadata is not defined at runtime. Separate multiple items with a comma.
# Example:
# SHOW_EXTRACTED_METADATA=file_path,language
SHOW_EXTRACTED_METADATA=has_accuracy_issues,accuracy_issues_count,undocumented_functions,undocumented_functions_count,parameter_mismatches,parameter_mismatches_count,incorrect_internal_refs,incorrect_internal_refs_count,accuracy_summary
```
